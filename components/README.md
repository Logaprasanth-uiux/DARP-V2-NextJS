# Components Directory

Place all shared presentation/UI components here (e.g. Buttons, Cards, Modals, Inputs).
All components should utilize CSS Modules for styling, consistent with the design tokens.
